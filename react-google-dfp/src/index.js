import SlotProvider from './components/SlotProvider';
import AdSlot from './components/AdSlot';

export default {
    SlotProvider: props => <SlotProvider {...props} />,
    AdSlot: props => <AdSlot {...props} />
};