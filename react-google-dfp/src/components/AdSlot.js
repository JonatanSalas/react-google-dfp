import React from 'react';
import PropTypes from 'prop-types';

export default class AdSlot extends React.Component {
    static propTypes = {

    };

    static defaultProps = {

    };

    render() {
        return (
            <div>
                <div {...this.props} />
            </div>
        )
    }
}